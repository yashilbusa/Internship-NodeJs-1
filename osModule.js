const os = require('os');

console.log(os.homedir());      // /home/yashil_busa
console.log(os.hostname());     // innovatemr
console.log(os.machine());      // x86_64
console.log(os.type());         // Linux
console.log(os.totalmem());     // Ram in Bytes : 16721780736 is Equivalent to 16.72 Gigabytes
console.log(os.freemem());      // It Shows Free Ram Memory in bytes


